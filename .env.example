# Copy to .env and fill in. Without a key the app runs with a stub LLM
# (pipeline works end-to-end, answers are canned summaries of the evidence).
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small
